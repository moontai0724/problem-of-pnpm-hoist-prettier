const exampleVariableAboutPrettierFormatDifference1: Required<string> =
  JSON.stringify({
    test: "value",
  });

// with prettier@1.19.1 installed in redis@3.0.0

const exampleVariableAboutPrettierFormatDifference2: Required<string> = JSON.stringify(
  {
    test: "value",
  }
);
